name: steering-recovery
channels:
  - pytorch
  - nvidia
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pytorch>=2.2
  - numpy>=1.26
  - pip:
      - datasets>=3.0
      - hydra-core>=1.3,<1.4
      - matplotlib>=3.8
      - omegaconf>=2.3,<2.4
      - safetensors>=0.4
      - tqdm>=4.66
      - transformers>=4.47
      - wandb>=0.18
      - pytest>=8
      - pytest-cov>=5
      - -e .
